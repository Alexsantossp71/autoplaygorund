# =========================================
#  autoplaygorund — dependências (Python 3.11+)
# =========================================
flask>=3.0,<4
flask-cors>=4.0,<5
openai>=1.0,<2
gunicorn>=21.0,<24
